package com.virality.dishly.service.mapper;

import com.virality.dishly.domain.User;
import com.virality.dishly.domain.Users;
import com.virality.dishly.service.dto.UserDTO;
import com.virality.dishly.service.dto.UsersDTO;
import java.util.List;
import java.util.Objects;

/**
 * Mapper for the entity {@link Users} and its DTO called {@link UsersDTO}.
 *
 * Normal mappers are generated using MapStruct, this one is hand-coded as MapStruct
 * support is still in beta, and requires a manual step with an IDE.
 */
public class UsersMapper {
    //    public List<UsersDTO> usersToUsersDTOs(List<Users> users) {
    //        return users.stream().filter(Objects::nonNull).map(this::usersToUsersDTO).toList();
    //    }
    //
    //    public UserDTO usersToUsersDTO(Users users) {
    //        return new UsersDTO(users);
    //    }

}
